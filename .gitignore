# binaries (root-only so cmd/repolink/ dir isn't swallowed)
/bin/
/dist/
/repolink
/repolink-*

# GoReleaser output
dist/

# Taskfile fingerprint cache (https://taskfile.dev/usage/#prevent-unnecessary-work)
.task/

# air live-reload working dir
tmp/

# go
*.test
*.out
coverage.*
vendor/

# ent generated (regenerable)
# keep generated under version control per ent convention — comment out to ignore
# internal/ent/*.go

# local state
*.db
*.db-journal
*.db-wal
*.db-shm

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# env
.env
.env.local
